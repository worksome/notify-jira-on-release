{
    "name": "send-commits-to-jira",
    "version": "5.0.0",
    "description": "A Github Action that takes a Github push event, parses the commits and sends them to Jira one by one.",
    "main": "dist/index.js",
    "dependencies": {
        "@actions/core": "^1.10.0",
        "node-fetch": "^2.6.7"
    },
    "devDependencies": {
        "@types/node": "^18.11.9",
        "@types/node-fetch": "^2.6.2",
        "@vercel/ncc": "^0.34.0",
        "typescript": "^4.9.3"
    },
    "scripts": {
        "build": "ncc build --license licenses.txt src/index.ts -o dist",
        "test": "echo \"Error: no test specified\" && exit 1",
        "bump": "npm version"
    },
    "keywords": [],
    "author": "",
    "license": "ISC"
}
